<div class="word-box">
  <div class="word-box-title">Discussion Question</div>
    <br><br>
    <div class="bigger-friendly">
      <%= @discussion_question.question_text %>
    </div>
    <div class="big-friendly">
      (by <%= link_to "#{@question_asker.username}", user_url(@question_asker) %>)
      <br><br><br>
      <% if @responses.present? %>
        <div class="bigger-friendly">Responses:</div>
        <br>
        <% @responses.each do |r| %>
          <%= r.response_text %> –<%= link_to "#{r.user.username}", user_url(r.user) %>
          <br><br>
        <% end %>
      <% else %>
        <i>No responses yet.</i>
        <br><br><br>
      <% end %>
    </div>
  </div>
</div>